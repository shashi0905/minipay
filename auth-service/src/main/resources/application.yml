spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:postgresql://localhost:5432/minipay_auth
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        default_schema: auth
        format_sql: true

server:
  port: 8082
  servlet:
    context-path: /

# JWT Configuration
jwt:
  secret: your-secret-key-change-this-in-production-use-strong-random-string-at-least-32-characters
  expiration: 86400000
  token:
    prefix: Bearer
